<div class="form">
  <%= simple_form_for [@period, @monument], html: { autocomplete: 'off' } do |f| %>
    <%= f.input :name, label: "Nom" %>
    <%= f.input :address, label: "Adresse",
  input_html: {data: {address_autocomplete_target: "address"}, class: "d-none"},
  wrapper_html: {data: {controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}} %>
    <%= f.input :description %>
    <%= f.input :wikipedia_url, label: "Page Wikipédia" %>
    <%= f.input :google_earth_url, label: "Lien vers Google Earth" %>
    <%= f.input :photo, as: :file %>
    <%= f.submit 'Ajouter un monument sur la carte', class: "btn btn-devise" %>
    <% end %>
</div>
